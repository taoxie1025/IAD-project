<br><br><br>

<% flash.each do |message_type, message| %>
  <div class="alert alert-<%= message_type %>"><%= message %></div>
<% end %>

<!--<%# provide(:title, current_user.image) %>-->

<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
    
      
      <%= label_tag  "#{current_user.name}" %>
       <br>
      <%= form_for @user, method: :put,  html: { multipart: true } do %>
       
       
        <%= image_tag "#{current_user.image.url(:medium)}" %>
        <br>
        <%= file_field_tag :image %>
        <br>
        <%= submit_tag("Update", class: "btn btn-primary") %>
    <% end %>
          
          
      <!-- <div class="col-md-6 col-md-offset-3">
    
    <#%= form_for @user, html: {multipart: true} do |f| %>
    <h2>
    <#%= current_user.name %>
    </h2>
      <#%= image_tag "#{current_user.image.url(:medium)}" %>
      <br>
      ##<#%= link_to "Home", root_path %>
      # => <a href="/">Home</a>

      <#%= f.submit "Update my account", class: "btn btn-primary" %>
    <#% end %>-->
  </div>    
          
    </section>
   
  </aside>
  <div class="col-md-8">
    <% if current_user.posts.any? %>
      <h3>Posts (<%= current_user.posts.count %>)</h3>
      <ol class="microposts">
        <%= render @posts %>
      </ol>
      <%= will_paginate @posts %>
    <% end %>
  </div>
</div>
